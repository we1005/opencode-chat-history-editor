{
  "name": "opencode-chat-history-editor",
  "version": "1.0.0",
  "private": true,
  "description": "In-place OpenCode conversation editor, based on KoniKee/opencode-sessions",
  "engines": { "node": ">=22" },
  "scripts": {
    "setup": "npm ci --prefix backend && npm ci --prefix frontend",
    "dev": "node scripts/dev.mjs",
    "opencode": "opencode serve --hostname 127.0.0.1 --port 4096",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "test": "npm run build --prefix backend && node --test backend/test/*.test.cjs && npm test --prefix frontend",
    "test:integration": "npm run build && node scripts/integration.mjs",
    "test:startup": "node --test scripts/start.test.mjs"
  }
}
